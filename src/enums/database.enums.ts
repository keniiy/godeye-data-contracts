/**
 * Database-related enums
 */

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
  ASCENDING = 1,
  DESCENDING = -1
}

export enum DatabaseType {
  TYPEORM = 'typeorm',
  MONGOOSE = 'mongoose'
}